{
  "name": "lottery-customer-app",
  "version": "1.0.0",
  "description": "Lotería Customer App (Android WebView)",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build": "cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^6.0.2"
  }
}
